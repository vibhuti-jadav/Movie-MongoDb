<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Add New Movie</title>

  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap"
    rel="stylesheet"
  />

  <style>
    /* ===== Base Layout ===== */
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      width: 100%;
      font-family: "Poppins", sans-serif;
      background: url('https://images.unsplash.com/photo-1524985069026-dd778a71c7b4?auto=format&fit=crop&w=1400&q=80')
        center/cover no-repeat;
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
      overflow: hidden;
    }

    /* Overlay */
    body::before {
      content: "";
      position: absolute;
      inset: 0;
      background: rgba(0, 0, 0, 0.7);
      backdrop-filter: blur(5px);
      z-index: 0;
    }

    /* ===== Form Box ===== */
    .form-container {
      position: relative;
      z-index: 1;
      background: rgba(255, 255, 255, 0.08);
      backdrop-filter: blur(12px);
      border-radius: 20px;
      padding: 40px 35px;
      width: 100%;
      max-width: 750px;
      animation: fadeIn 0.7s ease forwards;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    h2 {
      text-align: center;
      color: #f5c518;
      font-weight: 700;
      margin-bottom: 25px;
      text-shadow: 0 0 8px rgba(245, 197, 24, 0.7);
    }

    label {
      font-weight: 600;
      color: #f5c518;
    }

    .form-control {
      background: rgba(255, 255, 255, 0.1);
      border: none;
      border-radius: 10px;
      padding: 10px 14px;
      color: #fff;
      transition: 0.3s ease;
    }

    .form-control:focus {
      background: rgba(255, 255, 255, 0.2);
      box-shadow: 0 0 10px rgba(245, 197, 24, 0.5);
      color: #fff;
    }

    ::placeholder {
      color: black !important;
    }

    /* ===== Submit Button ===== */
    .btn-submit {
      background: linear-gradient(90deg, #f5c518, #ffdb4d);
      color: #000;
      border: none;
      border-radius: 50px;
      font-weight: 700;
      padding: 12px;
      transition: all 0.3s ease;
      box-shadow: 0 0 20px rgba(245, 197, 24, 0.4);
      width: 100%;
    }

    .btn-submit:hover {
      transform: scale(1.05);
      box-shadow: 0 0 40px rgba(245, 197, 24, 0.8);
    }

    /* ===== Back Button ===== */
    .back-btn {
      position: absolute;
      top: 20px;
      left: 20px;
      background: rgba(0, 0, 0, 0.6);
      color: #f5c518;
      border: none;
      padding: 10px 18px;
      border-radius: 30px;
      font-weight: 600;
      transition: 0.3s;
      z-index: 2;
    }

    .back-btn:hover {
      background: #f5c518;
      color: #000;
      transform: scale(1.05);
    }

    /* ===== Responsive ===== */
    @media (max-width: 768px) {
      .form-container {
        padding: 30px 20px;
      }
      h2 {
        font-size: 1.6rem;
      }
    }
  </style>
</head>
<body>

  <a href="/movie/list" class="back-btn">← Back</a>

  <div class="form-container">
    <h2>🎬 Add New Movie</h2>
    <form action="/movie/add" method="POST" enctype="multipart/form-data">
      <div class="mb-3">
        <label class="form-label">Title</label>
        <input type="text" name="title" class="form-control" placeholder="Enter movie title" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Description</label>
        <textarea name="description" class="form-control" rows="3" placeholder="Write a short description" required></textarea>
      </div>

      <div class="mb-3">
        <label class="form-label">Release Year</label>
        <input type="text" name="realeaseYear" class="form-control" placeholder="e.g. 2025" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Genre (comma separated)</label>
        <input type="text" name="genere" class="form-control" placeholder="Action, Thriller, Comedy" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Director</label>
        <input type="text" name="director" class="form-control" placeholder="Enter director name" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Cast (comma separated)</label>
        <input type="text" name="cast" class="form-control" placeholder="Actor 1, Actor 2" required>
      </div>

      <div class="mb-4">
        <label class="form-label">Poster Image</label>
        <input type="file" name="file" class="form-control">
      </div>

      <button type="submit" class="btn btn-submit">Add Movie</button>
    </form>
  </div>

</body>
</html>
