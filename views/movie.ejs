<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>🎬 Movie Collection</title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap"
    rel="stylesheet"
  />
  <style>
    /* ---------- Base Styles ---------- */
    body {
      background: radial-gradient(circle at top, #1c1c1c 0%, #000 90%);
      color: #fff;
      font-family: "Poppins", sans-serif;
      overflow-x: hidden;
      min-height: 100vh;
    }

    h1 {
      font-weight: 700;
      color: #f5c518;
      letter-spacing: 1px;
      text-shadow: 0 0 10px rgba(245, 197, 24, 0.6);
    }

    /* ---------- Movie Card ---------- */
    .movie-card {
      position: relative;
      border-radius: 15px;
      overflow: hidden;
      background: #111;
      box-shadow: 0 0 25px rgba(0, 0, 0, 0.6);
      transition: transform 0.4s ease, box-shadow 0.4s ease;
      cursor: pointer;
      height: 400px;
    }

    .movie-card:hover {
      transform: scale(1.05);
     box-shadow: rgba(0, 0, 0, 0.24) 0px 3px 8px;
    }

    .movie-card img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
      transition: transform 0.6s ease;
    }

    .movie-card:hover img {
      transform: scale(1.1);
      filter: blur(2px) brightness(0.6);
    }

    /* ---------- Overlay Text Area ---------- */
    .movie-info {
      position: absolute;
      bottom: 0;
      left: 0;
      width: 100%;
      height: 100%;
      padding: 25px;
      background: linear-gradient(180deg, rgba(0,0,0,0.2) 20%, rgba(0,0,0,0.95) 100%);
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      opacity: 0;
      visibility: hidden;
      transition: opacity 0.4s ease, visibility 0.4s ease;
    }

    .movie-card:hover .movie-info {
      opacity: 1;
      visibility: visible;
    }

    .movie-info h5 {
      color: #f5c518;
      font-weight: 600;
      margin-bottom: 6px;
    }

    .movie-info p {
      font-size: 0.9rem;
      line-height: 1.4;
      color: #ddd;
      margin-bottom: 4px;
    }

    .movie-info strong {
      color: #f5c518;
    }

    /* ---------- Buttons ---------- */
.action-btns {
  display: flex;
  justify-content: space-between;
  gap: 10px;        /* space between buttons */
  width: 100%;
  margin-top: 10px;
}

.action-btns a,
.action-btns button {
  flex: 1;          /* optional explicit width */
}
    .btn-warning {
      background-color: #f5c518;
      border: none;
      color: #000;
      font-weight: 600;
      transition: background 0.3s ease;
    }

    .btn-warning:hover {
      background-color: #ffdb4d;
    }

    .btn-danger {
      background-color: #e50914;
      border: none;
      font-weight: 600;
    }

    .btn-danger:hover {
      background-color: #ff1b2d;
    }

    .btn-add {
      background-color: #f5c518;
      font-weight: 600;
      color: #000;
      border-radius: 50px;
      padding: 10px 20px;
      transition: all 0.3s ease;
    }

    .btn-add:hover {
      background-color: #ffdb4d;
      transform: scale(1.05);
    }

    /* ---------- No Movie Section ---------- */
    .no-movies {
      margin-top: 100px;
      color: #aaa;
      font-size: 1.3rem;
    }

    /* ---------- Animation ---------- */
    .movie-card {
      animation: fadeUp 0.6s ease forwards;
      opacity: 0;
    }

    @keyframes fadeUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body>
  <div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1>🎬 Movie Collection</h1>
      <a href="/movie/addMovie" class="btn btn-add shadow">+ Add Movie</a>
    </div>

    <div class="row g-4">
      <% if (movie && movie.length > 0) { %>
        <% movie.forEach(movie => { %>
          <div class="col-md-3 col-sm-6">
            <div class="movie-card">
              <img src="<%= movie.file || '/default-poster.jpg' %>" alt="<%= movie.title %> poster" />
              <div class="movie-info">
                <h5><%= movie.title %></h5>
                <p><%= movie.description.substring(0, 80) %>...</p>
                <p><strong>Director:</strong> <%= movie.director %></p>
                <p><strong>Genre:</strong> <%= (movie.genere || []).join(', ') %></p>
                <div class="action-btns">
                  <a href="/movie/edit/<%= movie._id %>" class="btn btn-sm btn-warning">Edit</a>
                  <form action="/movie/delete/<%= movie._id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this movie?');">
                    <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                  </form>
                </div>
              </div>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <div class="col-12 text-center no-movies">
          <h4>No movies added yet 😢</h4>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>
